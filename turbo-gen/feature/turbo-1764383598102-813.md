# FIX: Update CI/CD workflow for Rust Windows build

This PR updates the `GitHub Actions` workflow to address the `Rust` build failures on `Windows` runners. I've added explicit steps to install the `MSVC` toolchain components necessary for `Rust` compilation and adjusted the linker flags. The `Windows` build now completes successfully on CI.

Generated by Turbo Bot