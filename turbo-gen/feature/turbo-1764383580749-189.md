# FEAT: Implement asynchronous logging in Python core module

The `Python` `core/logging.py` module currently uses a synchronous file handler, which can block the main thread under high logging volumes. This PR refactors the logging setup to use an `asynchronous` logging handler. This change ensures that log writes do not impede application performance, especially in I/O-bound scenarios.

Generated by Turbo Bot