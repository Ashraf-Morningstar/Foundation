# FEAT: Add new date utility functions in TypeScript core

This change introduces new `TypeScript` utility functions for date and time manipulation, specifically `formatDate` and `parseDate`. These functions leverage `date-fns` internally to provide robust and localized date operations. Unit tests cover various formats and edge cases to ensure reliability.

Generated by Turbo Bot